#Grok patterns for VEMCO data
VEMCO_NO_DETECTION %{YEAR:year}\-%{MONTHNUM:month}\-%{MONTHDAY:day}\T%{TIME}%{ISO8601_TIMEZONE}\|%{WORD:vemco_id}\|%{INT:serial_number}\,%{INT:line_count}\,%{YEAR:year}\-%{MONTHNUM:month}\-%{MONTHDAY:day}%{SPACE}%{TIME}\,%{WORD:instrument_mode}\,DC=%{INT:tag_detection_count}\,PC=%{INT:ping_count}\,LV=%{NUMBER:line_voltage}\,BV=%{NUMBER:battery_voltage}\,BU=%{NUMBER:battery_usage}\,I=%{NUMBER:impedence}\,T=%{NUMBER:internal_sensor_temperature}\,DU=%{NUMBER:detection_memory_used}\,RU=%{NUMBER:raw_memory_usage}\,XYZ=%{NUMBER:tilt_x}\:%{NUMBER:tilt_y}\:%{NUMBER:tilt_z}
VEMCO_DETECTION %{YEAR:year}\-%{MONTHNUM:month}\-%{MONTHDAY:day}\T%{TIME}%{ISO8601_TIMEZONE}\|%{WORD:vemco_id}\|%{INT:serial_number}\,%{INT:line_count}\,%{YEAR:year}\-%{MONTHNUM:month}\-%{MONTHDAY:day}%{SPACE}%{TIME}\,%{USERNAME:fish_tag_model}\,%{INT:fish_tag_id}
